THIRD PARTY LICENSES
====================

This file lists the licenses of the projects used in atlas-local-lib-py.

OVERVIEW OF LICENSES:
{{#each overview}}
- {{name}} ({{count}} dependencies)
{{/each}}

{{#each licenses}}
-----------------------------------------------------------------------------
                        {{name}}
        applies to:
{{#each used_by}}
        - {{crate.name}} {{crate.version}}{{#if crate.repository}} ({{crate.repository}}){{else}} (https://crates.io/crates/{{crate.name}}){{/if}}
{{/each}}
-----------------------------------------------------------------------------

{{{text}}}

{{/each}}
