version: 2
updates:
  # Enable version updates for Rust/Cargo dependencies
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "chore(deps)"
    labels:
      - "dependencies"
    # Group updates by type to separate security from version updates
    groups:
      rust-security:
        applies-to: security-updates
        patterns:
          - "*"
      rust-version-updates:
        applies-to: version-updates
        patterns:
          - "*"
    cooldown:
      default-days: 7

  # Enable version updates for Python dependencies and build/dev tooling
  # (e.g. maturin, pytest), sourced from pyproject.toml
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "chore(deps)"
    labels:
      - "dependencies"
    # Group updates by type to separate security from version updates
    groups:
      python-security:
        applies-to: security-updates
        patterns:
          - "*"
      python-version-updates:
        applies-to: version-updates
        patterns:
          - "*"
    cooldown:
      default-days: 7

  # Enable version updates for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    open-pull-requests-limit: 5
    commit-message:
      prefix: "chore(deps)"
    labels:
      - "dependencies"
    cooldown:
      default-days: 7
