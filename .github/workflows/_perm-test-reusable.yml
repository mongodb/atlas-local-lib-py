name: perm-test-reusable
on: workflow_call
permissions:
  contents: read
  pull-requests: write
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "reached the job — validation passed"