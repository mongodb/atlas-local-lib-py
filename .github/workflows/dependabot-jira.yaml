# synced from apix-actions/.github/workflows/dependabot-jira.yaml
name: Create Jira Ticket for Dependabot PRs

on:
  pull_request:
    types: [opened]

permissions: {}

jobs:
  create-jira:
    if: github.actor == 'dependabot[bot]'
    permissions:
      contents: read
      pull-requests: write
    uses: mongodb/apix-action/.github/workflows/_dependabot-jira.yaml@e5d48b81974f46faf18c2615ae56fd2ff821f8e6 # main
    secrets: inherit