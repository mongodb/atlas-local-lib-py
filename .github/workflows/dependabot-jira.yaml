# synced from apix-actions/.github/workflows/dependabot-jira.yaml
name: Create Jira Ticket for Dependabot PRs

on:
  pull_request:
    types: [opened]

permissions: {}

jobs:
  create-jira:
    if: github.actor == 'dependabot[bot]'
    permissions:
      contents: read
      pull-requests: write
    uses: mongodb/apix-action/.github/workflows/_dependabot-jira.yaml@8c5007247643059c0d15c061290a345eb987657f # main
    secrets: inherit