# synced from apix-actions/.github/workflows/close-jira.yaml
name: Auto Close Jira

on:
  pull_request:
    types: [closed]

permissions: {}

jobs:
  close-jira:
    if: contains(github.event.pull_request.labels.*.name, 'auto_close_jira')
    permissions:
      contents: read
      pull-requests: read
    uses: mongodb/apix-action/.github/workflows/_close-jira.yaml@7a5e504fa3e5d38a29ebe942bd675d6aa7c434c7 # main
    secrets: inherit